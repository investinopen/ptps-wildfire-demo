// near-black, like a topo map -- every brighter hue is already taken by a hazard/risk layer, and it stays legible when printed in grayscale
export const ROAD = "#2b2b2b";
// mid gray, so a state line reads as secondary to the roads it crosses; used for its legend swatch too
export const STATE_LINE = "#666666";
